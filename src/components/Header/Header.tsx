import './Header.scss';

const Header: React.FC = () => {
    return (
        <header className="header">
            <h1 className="header__text">MANAGE YOUR PROJECTS!</h1>
        </header>
    )
};

export default Header;